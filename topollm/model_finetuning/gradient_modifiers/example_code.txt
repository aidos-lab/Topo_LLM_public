```
for name, param in model.named_parameters():
     if name.startswith("..."): # choose whatever you like here
        param.requires_grad = False
```

Inspect whether the freezing worked:
```
for name, param in model.named_parameters():
     print(name, param.requires_grad)
```