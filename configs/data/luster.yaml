defaults:
  - data_splitting: do_nothing
  - data_subsampling: train_split_and_random
  - _self_

# TODO: Implement concatenating the 'source' and 'target' columns for the LUSTER dataset
column_name: source
context: dataset_entry

dataset_description_string: luster_column=${data.column_name}
dataset_path: csv

data_dir: ${paths.luster_repository_base_path}/data/datasets/data_dfs
